apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: influxdb-claim
spec:
  storageClassName: openebs-cstor-sparse
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 40G
